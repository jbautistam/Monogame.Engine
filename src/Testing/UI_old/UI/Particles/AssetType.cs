namespace ParticleEngine.Core;

// ============================================================================
// DEFINITIONS / CURVES
// ============================================================================

namespace ParticleEngine.Definitions;

public enum AssetType
{
    Texture, TextureAtlas, Shader, Audio, Curve, Gradient,
    Shape, Material, ParticleSystem, SubEmitter, VectorField
}
