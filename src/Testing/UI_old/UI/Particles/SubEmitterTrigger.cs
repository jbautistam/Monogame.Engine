namespace ParticleEngine.Core;

// ============================================================================
// DEFINITIONS / CURVES
// ============================================================================

namespace ParticleEngine.Definitions;

public enum SubEmitterTrigger
{
    OnBirth, OnDeath, OnCollision, OnBounce, OnFirstCollision,
    OnStop, OnVisible, OnInvisible, Timer
}
